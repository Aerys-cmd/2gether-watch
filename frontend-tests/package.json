{
  "name": "2gether-watch-frontend-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Real-browser Playwright tests for 2gether Watch WebRTC flows",
  "scripts": {
    "test": "playwright test",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "PWDEBUG=1 playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}

